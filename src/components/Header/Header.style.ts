import styled from "styled-components";

const Header = styled.header`
  width: 100%;
  background: #181829;

  padding: 32px;
  margin-bottom: 32px;
`;

const Title = styled.h1`
  color: #fff;

  margin-bottom: 16px;
`;

const Paragraph = styled.p`
  color: rgba(255, 255, 255, 0.5);
`;

export default { Title, Header, Paragraph };